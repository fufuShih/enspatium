{
  "name": "enspatium",
  "private": true,
  "packageManager": "pnpm@12.0.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=12.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @enspatium/server dev",
    "generate:api": "pnpm --filter @enspatium/server generate:openapi && pnpm --filter @enspatium/web generate:api",
    "build": "pnpm --recursive build",
    "start": "pnpm --filter @enspatium/server start",
    "test": "pnpm --recursive test",
    "typecheck": "pnpm --recursive typecheck"
  }
}
